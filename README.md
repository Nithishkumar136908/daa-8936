Conclusion:
Thus, the factorial program was successfully implemented using iterative and recursive methods. 
Both methods have O(n) time complexity, but the iterative method uses less memory (O(1)) compared to the recursive method (O(n)). 
This practical helped in understanding time and space complexity analysis in DAA.
